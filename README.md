# MNIST_NN
Neural Network model on the MNIST image dataset. The model can be trained with a GPU or CPU.
First model has an architecture of (784, 32, 10) with one hidden layer of 10 neurons. Test accuracy is 96.9% after training for 10 epochs on the CPU with mini-batch SGD.
